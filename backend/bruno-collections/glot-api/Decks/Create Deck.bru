meta {
  name: Create Deck
  type: http
  seq: 10
}

post {
  url: {{base_url}}/api/v1/decks
  body: json
}

body:json {
  {
    "name": "French Vocabulary",
    "description": "Basic French words and phrases"
  }
}

tests {
  test("Status is 201", function() {
    expect(res.status).to.equal(201);
  });
  
  test("Deck has ID", function() {
    expect(res.body.id).to.be.a("number");
  });
}

script:post-response {
  bru.setVar("deck_id", res.body.id);
}
