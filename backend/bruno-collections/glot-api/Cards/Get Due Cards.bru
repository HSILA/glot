meta {
  name: Get Due Cards
  type: http
  seq: 4
}

get {
  url: {{base_url}}/api/v1/cards/due
}

params:query {
  limit: 20
}

tests {
  test("Status is 200", function() {
    expect(res.status).to.equal(200);
  });
  
  test("Returns array", function() {
    expect(res.body).to.be.an("array");
  });
}
